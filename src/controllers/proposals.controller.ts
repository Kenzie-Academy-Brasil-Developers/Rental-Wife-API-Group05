import { Request, Response } from "express";

// export const proposalController = async (req: Request, res: Response) => {
//   const data = await proposalservice(req.body);

//   return res.status(200).json();
// };
